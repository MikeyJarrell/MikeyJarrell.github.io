# Hugo build output — regenerated by GitHub Actions on every push
public/
resources/
.hugo_build.lock

# macOS metadata
.DS_Store
**/.DS_Store

# Stray PDF-conversion artifact (don't deploy)
static/pdf/cv.md
